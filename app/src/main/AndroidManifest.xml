<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pathum.kdu">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StudentComActivity" />
        <activity android:name=".LecturerActivity" />
        <activity android:name=".Dashboard" />
        <activity android:name=".Elibrary" />
        <activity android:name=".Contact" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".Password" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ViewUploadsActivity" />
        <activity android:name=".LocationActivity" />
        <activity android:name=".EventActivity" />
        <activity android:name=".Circle1Activity" />
        <activity android:name=".Circle2Activity" />
        <activity android:name=".StudentMainActivity" />
        <activity android:name=".LecturerMainActivity" >
        <activity android:name=".Lecturertwomain" >
        </activity>



    </application>

</manifest>
